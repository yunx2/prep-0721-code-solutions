const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];
console.log('languages array', languages);

const firstElement = languages[0];
console.log('this first element of the array is', firstElement);

const thirdElement = languages[2];
console.log('this third element of the array is', thirdElement);

const length = languages.length;
console.log('the length of the array is', length);

const lastElement = languages[length - 1];
console.log('this last element of the array is', lastElement);
